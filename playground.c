#include <stdio.h>
#include <stdlib.h>
#include <string.h>
// #include <time.h>
// #include "./Util/rbtree.h"
#include "./Lib/build_bug.h"

int main()
{

  // Result: strings are equal
  int value = strcmp("str1", "str1");
  if (value == 0)
    printf("strings are same\n");
  else
    printf("strings are not same\n");
  return 0;

  return 0;
}